{
  "name": "schema",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "main": "./dist/index.js",
  "source": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist/*"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "dev": "tsc -w",
    "lint": "eslint \"src/**/*.ts*\"",
    "test": "jest"
  },
  "devDependencies": {
    "typescript": "^4.8.3",
    "tsconfig": "workspace:*"
  },
  "dependencies": {
    "zod": "^3.21.4"
  }
}
